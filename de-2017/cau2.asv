d = randint(1,10);
R = 1e6; %toc do bit
Ns = 256; %so mau bieu dien 1 ky hieu

Tb = 1/R; %chu ki bit
Nb = length(d); %so luong bit
Timewindow = Nb*Tb; %tong thoi gian bieu dien
ts = Timewindow/(Ns - 1); %chu ki lay mau
t = 0:ts:Timewindow; %vector thoi gian

y = zeros(size(t));
code = [];
count = 0;
for i=1:Ns
    n = fix(t(i)/Tb)+1;
    if n>=Nb
        n = Nb;
    end
    if(mod(count,2) == 0)
        y(i) = 2*d(n)-1;
        code(n) = 2*d(n)-1;
    else if (mod(count,2))
end
plot(t,y)

